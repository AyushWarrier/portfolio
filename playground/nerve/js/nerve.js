/* =============================================================
   NERVE — page script.
   Nerve is a static, editorial Playground page: the diagrams are
   built to read like images (no hover, no motion), and the screen
   gallery is plain markup. Nothing here needs to run on load.

   Shared behaviour still comes from the portfolio's own scripts,
   loaded alongside this file: nav.js (mobile menu) and main.js
   (copy-email toast).

   This file is the page's own scoped hook — intentionally empty
   for now, kept so any Nerve-specific enhancement added as the
   page grows, section by section, has a single, obvious home
   that degrades gracefully if it never loads.
   ============================================================= */

(function () {
  'use strict';

  // No page-specific behaviour yet — see note above.
})();
